services:
  postgres:
    image: postgres:15
    container_name: postgres-store-manager
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password  
      POSTGRES_DB: store_manager
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: